<% content_for :title, "Configurações - FinTrack" %>

<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="text-light mb-1">
      <i class="bi bi-gear me-2 text-primary"></i>
      Configurações da Conta
    </h1>
    <p class="text-muted mb-0">Gerencie suas informações pessoais e preferências</p>
  </div>
</div>

<!-- Main Content -->
<div class="row">
  <div class="col-12">
    <div class="card bg-secondary border-0">
      <div class="card-body">
          <div class="row g-4">
            <!-- Informações da Conta -->
            <div class="col-lg-8">
              <div class="card bg-dark border-primary">
                <div class="card-header bg-primary text-white border-0">
                  <h5 class="mb-0">
                    <i class="bi bi-person-circle me-2"></i>
                    Informações Pessoais
                  </h5>
                </div>
                <div class="card-body">
                  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), 
                               html: { method: :put, class: "needs-validation", novalidate: true }) do |f| %>
                    <%= render "devise/shared/error_messages", resource: resource %>

                    <div class="row">
                      <div class="col-md-6 mb-3">
                        <%= f.label :first_name, "Nome", class: "form-label text-light" %>
                        <%= f.text_field :first_name, class: "form-control bg-secondary text-light border-secondary", 
                                         placeholder: "Digite seu nome" %>
                      </div>
                      <div class="col-md-6 mb-3">
                        <%= f.label :last_name, "Sobrenome", class: "form-label text-light" %>
                        <%= f.text_field :last_name, class: "form-control bg-secondary text-light border-secondary", 
                                         placeholder: "Digite seu sobrenome" %>
                      </div>
                    </div>

                    <div class="mb-3">
                      <%= f.label :username, "Nome de Usuário", class: "form-label text-light" %>
                      <%= f.text_field :username, class: "form-control bg-secondary text-light border-secondary", 
                                       placeholder: "Digite seu nome de usuário" %>
                    </div>

                    <div class="mb-3">
                      <%= f.label :email, "E-mail", class: "form-label text-light" %>
                      <%= f.email_field :email, autofocus: true, autocomplete: "email", 
                                        class: "form-control bg-secondary text-light border-secondary",
                                        placeholder: "Digite seu e-mail" %>
                      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                        <div class="form-text text-warning">
                          <i class="bi bi-exclamation-triangle me-1"></i>
                          Aguardando confirmação para: <%= resource.unconfirmed_email %>
                        </div>
                      <% end %>
                    </div>

                    <hr class="border-secondary my-4">
                    <h6 class="text-light mb-3">
                      <i class="bi bi-shield-lock me-2"></i>
                      Alterar Senha
                    </h6>

                    <div class="mb-3">
                      <%= f.label :password, "Nova Senha", class: "form-label text-light" %>
                      <%= f.password_field :password, autocomplete: "new-password", 
                                           class: "form-control bg-secondary text-light border-secondary",
                                           placeholder: "Deixe em branco para manter a atual" %>
                      <% if @minimum_password_length %>
                        <div class="form-text text-muted">
                          <i class="bi bi-info-circle me-1"></i>
                          Mínimo de <%= @minimum_password_length %> caracteres
                        </div>
                      <% end %>
                    </div>

                    <div class="mb-3">
                      <%= f.label :password_confirmation, "Confirmar Nova Senha", class: "form-label text-light" %>
                      <%= f.password_field :password_confirmation, autocomplete: "new-password", 
                                           class: "form-control bg-secondary text-light border-secondary",
                                           placeholder: "Confirme a nova senha" %>
                    </div>

                    <div class="mb-4">
                      <%= f.label :current_password, "Senha Atual", class: "form-label text-light" %>
                      <%= f.password_field :current_password, autocomplete: "current-password", 
                                           class: "form-control bg-secondary text-light border-secondary",
                                           placeholder: "Digite sua senha atual para confirmar" %>
                      <div class="form-text text-muted">
                        <i class="bi bi-shield-check me-1"></i>
                        Necessário para confirmar as alterações
                      </div>
                    </div>

                    <div class="d-grid">
                      <%= f.submit "Atualizar Informações", class: "btn btn-primary btn-lg" %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>

            <!-- Painel Lateral -->
            <div class="col-lg-4">
              <div class="row g-3">
                <!-- Resumo da Conta -->
                <div class="col-12">
                  <div class="card bg-dark border-primary">
                    <div class="card-header bg-info text-dark border-0">
                      <h6 class="mb-0">
                        <i class="bi bi-graph-up me-2"></i>
                        Resumo da Conta
                      </h6>
                    </div>
                    <div class="card-body">
                      <div class="row text-center g-3">
                        <div class="col-6">
                          <div class="border-end border-secondary">
                            <small class="text-success d-block">Receitas</small>
                            <strong class="text-success fs-5"><%= current_user.incomes.count %></strong>
                          </div>
                        </div>
                        <div class="col-6">
                          <small class="text-danger d-block">Despesas</small>
                          <strong class="text-danger fs-5"><%= current_user.expenses.count %></strong>
                        </div>
                      </div>
                      <hr class="border-secondary my-3">
                      <div class="text-center">
                        <small class="text-muted d-block">Membro desde</small>
                        <strong class="text-info">
                          <%= current_user.created_at.strftime("%d/%m/%Y") %>
                        </strong>
                      </div>
                    </div>
                  </div>
                </div>

                <!-- Reset de Finanças -->
                <div class="col-12">
                  <div class="card bg-dark border-primary">
                    <div class="card-header bg-warning text-dark border-0">
                      <h6 class="mb-0">
                        <i class="bi bi-arrow-clockwise me-2"></i>
                        Reset Financeiro
                      </h6>
                    </div>
                    <div class="card-body">
                      <p class="text-muted small mb-3">
                        Remover todo o histórico de receitas e despesas.
                      </p>
                      <div class="d-grid">
                        <%= link_to reset_finances_path, 
                                    class: "btn btn-warning",
                                    data: { 
                                      turbo_method: :delete,
                                      confirm: "⚠️ ATENÇÃO! Esta ação irá remover PERMANENTEMENTE todas as receitas, despesas e todo o histórico financeiro. Esta ação não pode ser desfeita! Tem certeza que deseja continuar?"
                                    } do %>
                          <i class="bi bi-exclamation-triangle me-2"></i>
                          Resetar Finanças
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>

                <!-- Zona de Perigo -->
                <div class="col-12">
                  <div class="card bg-dark border-primary">
                    <div class="card-header bg-danger text-white border-0">
                      <h6 class="mb-0">
                        <i class="bi bi-exclamation-octagon me-2"></i>
                        Zona de Perigo
                      </h6>
                    </div>
                    <div class="card-body">
                      <p class="text-muted small mb-3">
                        Excluir permanentemente sua conta e todos os dados.
                      </p>
                      <div class="d-grid">
                        <%= button_to registration_path(resource_name), 
                                      class: "btn btn-danger",
                                      data: { 
                                        turbo_method: :delete,
                                        confirm: "🚨 ATENÇÃO - EXCLUSÃO PERMANENTE! Esta ação irá excluir sua conta permanentemente e remover TODOS os seus dados. Esta ação é IRREVERSÍVEL! Tem certeza?"
                                      } do %>
                          <i class="bi bi-trash me-2"></i>
                          Excluir Conta
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

        <!-- Botão Voltar -->
        <div class="text-center mt-4">
          <%= link_to settings_path, class: "btn btn-outline-primary" do %>
            <i class="bi bi-arrow-left me-2"></i>
            Voltar às Configurações
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
